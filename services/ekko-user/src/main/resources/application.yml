server:
  port: 8082

spring:
  application:
    name: ekko-user
  datasource:
    url: jdbc:postgresql://localhost:5433/userdb
    username: ekko
    password: ekko
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true